@using HNTAS.Web.UI.Models.HeatNetwork
@model HNDetailsViewModel

@{
    ViewData["Title"] = "Heat network Overview";
}

<div class="govuk-width-container">
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            <form asp-action="NetworkDetails" method="get">
                <span class="govuk-caption-l">@Model.UHNID @Model.Name</span>
                <h1 class="govuk-heading-l">
                    Network overview
                </h1>

                <dl class="govuk-summary-list">
                    @if (!string.IsNullOrEmpty(Model.Address.Fulladdress))
                    {
                        <div class="govuk-summary-list__row">
                            <dt class="govuk-summary-list__key">
                                Address
                            </dt>
                            <dd class="govuk-summary-list__value">
                                @Model.Address.Fulladdress
                            </dd>
                        </div>
                    }

                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Coordinates
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.Coordinates
                        </dd>
                    </div>
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Network type
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.NetworkType
                        </dd>
                    </div>
                    <div class="govuk-summary-list__row">
                        <dt class="govuk-summary-list__key">
                            Phase
                        </dt>
                        <dd class="govuk-summary-list__value">
                            @Model.Phase
                        </dd>
                    </div>
                </dl>

                <button type="submit" class="govuk-button" data-module="govuk-button">
                    Continue
                </button>
            </form>
        </div>
        </div>
</div>